import type { Service, Technician } from '../types'

interface Props { technician: Technician; services: Service[]; selected: Service; onSelect: (service: Service) => void; onBack: () => void; onBook: () => void }

export function TechnicianDetail({ technician: tech, services, selected, onSelect, onBack, onBook }: Props) {
  return <section className="screen active"><div className="detail-hero"><img src={tech.img} alt={tech.name}/><button className="back" onClick={onBack}>‹</button><span className="demo-badge">● 现在可接单</span><div className="hero-info"><h1>{tech.name} · {tech.title}</h1><div className="hero-meta"><span>★ {tech.rating}</span><span className="verified">✓ 平台认证</span></div></div></div><div className="detail-body"><p className="intro">{tech.intro}</p><div className="metric-row"><div className="metric"><strong>{tech.orders}</strong><small>累计服务</small></div><div className="metric"><strong>98%</strong><small>准时到达</small></div><div className="metric"><strong>6年+</strong><small>从业经验</small></div></div><h2 className="sub-title">近期可约</h2><div className="calendar-strip"><div className="slot-pill hot">今天<br/><b>19:00</b></div><div className="slot-pill">今天<br/><b>20:30</b></div><div className="slot-pill">明天<br/><b>15:00</b></div></div><h2 className="sub-title">选择服务</h2><div className="service-list">{services.map((service) => <button key={service.id} className={`service ${service.id === selected.id ? 'selected' : ''}`} onClick={() => onSelect(service)}><span><b>{service.name}</b><small>{service.desc}</small></span><span className="service-price">¥{service.price}<small>/次</small></span></button>)}</div><h2 className="sub-title">真实评价</h2><div className="review-card"><span className="stars">★★★★★</span><p>肩颈放松很到位，力度沟通也很细致。技师提前到达，用品都是当面拆封的。</p><small>匿名用户 · 3天前</small></div><div className="sticky-cta"><button className="primary" onClick={onBook}>立即预约 · ¥{selected.price}</button></div></div></section>
}
